import { getAuthURL } from "@/alfa/id"
import { getAccessToken } from "@/alfa/token"
import { getCompanyInfo } from "@/alfa/info"
import { createPayment } from "@/alfa/payments"

export { getAuthURL, getAccessToken, getCompanyInfo, createPayment }